# Metadata Specification

Every skill should contain metadata.yaml.

Example:

```yaml
difficulty: beginner

language_agnostic: true

priority: high

quality_score: 100
```

Purpose:

Metadata is used by the AI engine to classify and prioritize skills.